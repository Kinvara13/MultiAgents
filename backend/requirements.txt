# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17

# HTTP Client (for real Agent API calls)
httpx>=0.27.0

# Database (async PostgreSQL)
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0

# Redis (for caching and pub/sub)
redis>=5.2.0

# Validation & Configuration
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Utilities
python-dotenv>=1.0.0
tenacity>=9.0.0

# File Storage (optional)
minio>=7.2.0
aiofiles>=24.1.0

# JWT (for future auth)
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
